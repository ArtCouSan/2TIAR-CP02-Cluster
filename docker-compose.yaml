version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:80"  # Mapeia a porta do contêiner para a porta do host (altere conforme necessário)
    volumes:
      - .:/app       # Monta o diretório atual como volume dentro do contêiner
